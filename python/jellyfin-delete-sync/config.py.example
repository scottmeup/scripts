# fill the details, save this file as config.py and place it in the same directory as the main script

SONARR_URL = 'http://localhost:8989'
SONARR_API_KEY = 'your-sonarr-api-key-here'

RADARR_URL = 'http://localhost:7878'
RADARR_API_KEY = 'your-radarr-api-key-here'

LISTEN_HOST = '0.0.0.0'
LISTEN_PORT = 5373

# Optional: Auto-rebuild episode map every N minutes
# Set to None to disable, or e.g. 1440 for once per day, 60 for hourly
REBUILD_INTERVAL_MINUTES = None   # Change to 1440 or any number > 0 to enable
